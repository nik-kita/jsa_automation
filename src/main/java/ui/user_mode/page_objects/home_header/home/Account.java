package ui.user_mode.page_objects.home_header.home;

import ui.BasePageObject;
import ui.engine.OnixWebDriver;
import ui.user_mode.general_parts.Footer;

public class Account extends BasePageObject implements Footer {
    public Account(OnixWebDriver driver) {
        super(driver);
    }
}
